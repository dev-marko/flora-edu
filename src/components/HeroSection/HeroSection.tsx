import { Box, Flex } from '@chakra-ui/react';

const HeroSection = () => {
  return (
    <Box bg={'gray'}>
      <Flex>
        <Box bg={'whiteAlpha.300'}>
          text
        </Box>
      </Flex>
    </Box>
  );
};

export default HeroSection;
